# .env.example — copie para .env.local para desenvolvimento local
# Em produção, o Nginx faz o proxy e este arquivo não é necessário.

# URL base da API (usado apenas em desenvolvimento sem Docker)
VITE_API_BASE_URL=http://localhost:8000
